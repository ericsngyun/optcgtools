{
  "name": "optcg-cards-css",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "Authenticated OPTCG material extraction and physically grounded web/3D card rendering lab.",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "test:web": "playwright test",
    "test:node": "node --test tests-node/",
    "compile:profile": "node scripts/compile-card-profile.mjs",
    "render:sequence": "node scripts/render-sequence.mjs",
    "fetch:upstream": "bash scripts/fetch-upstream.sh",
    "check:source-pin": "node scripts/check-source-pin.mjs"
  },
  "dependencies": {
    "three": "0.185.0"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "@sveltejs/vite-plugin-svelte": "^1.1.0",
    "svelte": "^3.52.0",
    "vite": "^3.2.0"
  }
}
